import StackSection from "@/src/features/admin/stack/stack-section";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "SKILLS",
};

export default function Page() {
  return <StackSection />;
}
